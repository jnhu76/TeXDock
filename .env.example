# TeXDock 环境变量配置
#
# 用法：复制为 .env 并按实际修改
#   cp .env.example .env
#
# docker-compose.sandbox.yml 依赖本变量。
# 注意：docker-compose.yml（非沙箱）不依赖此变量，可不用。

# 宿主机数据目录的【绝对路径】。
# 必须与 docker-compose.yml 里 sharelatex 服务的 volume 挂载源一致。
# 沙箱编译的 SANDBOXED_COMPILES_HOST_DIR_* 在此基础上拼接子目录。
#
# 示例：
#   OVERLEAF_DATA_PATH=/home/hoo/sharelatex_data
OVERLEAF_DATA_PATH=/home/hoo/sharelatex_data
