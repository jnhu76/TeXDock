# services/

This directory is intentionally empty at initialization.

Service code is imported from the vendor ShareLaTeX image snapshot only after review.

Do not bulk-copy service code into this directory without an explicit import step.
