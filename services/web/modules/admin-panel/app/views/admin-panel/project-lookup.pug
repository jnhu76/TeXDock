extends ../../../../../app/views/layout-marketing

block content
	.content.content-alt#main-content
		.container
			.row
				.col-sm-12
					.card
						.card-body
							.page-header
								h1 Project URL Lookup

							form.mb-4(method='GET', action='/admin/project')
								.row
									.col-sm-8
										.input-group
											input.form-control(
												type='text'
												name='search'
												placeholder='Search by project ID or name…'
												value=search
											)
									.col-sm-4
										button.btn.btn-primary(type='submit') Search

							if projects.length > 0
								table.table.table-striped
									thead
										tr
											th Name
											th ID
								th Last Updated
									tbody
										each project in projects
											tr
												td
													a(href='/admin/project/' + project._id)= project.name
												td
													code= project._id
												td= project.lastUpdated ? new Date(project.lastUpdated).toLocaleDateString() : '-'
							else if search
								p No projects found.
